# pnpm CLI test

Run from this folder:

```bash
pnpm dlx copilotkit@latest create
pnpm dlx copilotkit@latest whoami
pnpm dlx copilotkit@latest project select
pnpm install
pnpm dev
```

Use a separate generated project name such as `cli-test-pnpm`.
