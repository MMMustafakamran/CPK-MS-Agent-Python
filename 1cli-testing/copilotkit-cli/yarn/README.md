# Yarn CLI test

```bash
yarn dlx copilotkit@latest create
yarn dlx copilotkit@latest whoami
yarn dlx copilotkit@latest project select
yarn install
yarn dev
```

Use a separate generated project name such as `cli-test-yarn`.
